# Flashback3000 plugin config.
#
# Voxy LOD chunks are wired up automatically by the Flashback3000 companion mod, which rewrites
# metadata.json on the client at download time to point at the local user's voxy storage
# (.voxy/saves/<server-address>). No server-side config required.
